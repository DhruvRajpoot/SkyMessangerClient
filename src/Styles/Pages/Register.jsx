import styled from "styled-components";
import { LoginContainer, LoginForm, PasswordVisibility } from "./Login";

export const RegisterContainer = styled(LoginContainer)``;

export const RegisterForm = styled(LoginForm)``;

